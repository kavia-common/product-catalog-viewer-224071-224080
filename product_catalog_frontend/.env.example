# React app environment variables
# Fill in your Supabase project credentials to enable live product data.
# If left blank, the app will use HTTP API (REACT_APP_API_BASE) or mock fallback via feature flag.

# Base URL for optional REST backend (kept for compatibility/fallback)
REACT_APP_API_BASE=http://localhost:8080/api

# Feature flags (comma-separated). Include "mock_api" to force using mock data.
REACT_APP_FEATURE_FLAGS=

# Supabase configuration (recommended)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=

# Optional logging level: debug | info | warn
REACT_APP_LOG_LEVEL=info

# Other optional template variables supported but unused directly here:
REACT_APP_BACKEND_URL=
REACT_APP_FRONTEND_URL=
REACT_APP_WS_URL=
REACT_APP_NODE_ENV=development
REACT_APP_ENABLE_SOURCE_MAPS=true
REACT_APP_PORT=3000
REACT_APP_TRUST_PROXY=false
REACT_APP_HEALTHCHECK_PATH=/healthz
REACT_APP_EXPERIMENTS_ENABLED=false
